{
  "name": "pte-prep-hub",
  "version": "1.0.0",
  "description": "NotebookLM-style chat interface over the PTE Academic handbook, backed by OpenAI.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "multer": "^2.0.0",
    "pdf-parse": "^1.1.4"
  }
}
